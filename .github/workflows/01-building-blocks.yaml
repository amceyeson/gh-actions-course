name: 01 - Building Blocks

on: workflow_dispatch
jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say Hello
              run: echo "Hello Platinum World Asshole"
    
    echo-goodbye:
        runs-on: Ubuntu-latest
        steps:
            - name: Failed step
              run: |
                echo "I will succeeed"
                exit 0
            - run: echo "Goodbye" 
